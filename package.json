{
  "name": "oink",
  "version": "0.0.1",
  "description": "A module for interacting with Onkyo recievers",
  "main": "lib/index.js",
  "author": "Brian Davidson",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^23.3.0",
    "@types/node": "^10.5.2",
    "jest": "^23.4.1",
    "rimraf": "^2.6.2",
    "ts-jest": "^23.0.1",
    "typescript": "^2.9.2"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rimraf ./lib",
    "prepublish": "yarn clean && yarn test && yarn build",
    "test": "jest",
    "watch": "tsc --watch & jest --watch"
  },
  "jest": {
    "transform": {
      "^.+\\.tsx?$": "ts-jest"
    },
    "testRegex": "/__tests__/.*\\.(jsx?|tsx?)$",
    "moduleFileExtensions": [
      "ts",
      "tsx",
      "js",
      "jsx",
      "json",
      "node"
    ]
  }
}
